apiVersion: v1
data:
  username: {{ .Values.db.credentials.username | b64enc | quote }}
  password: {{ randAlphaNum 12 | b64enc | quote }}
kind: Secret
metadata:
  annotations:
    "helm.sh/hook": "pre-install"
  name: {{ .Chart.Name }}-db-credentials
type: Opaque
